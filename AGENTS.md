# 小鱼精灵协作规则

## 开始工作前

1. 先读 [文档总入口](docs/README.md)。
2. 再按任务类型阅读对应的权威契约和平台说明，不要默认通读全部文档。
3. 查看目标模块自己的 `README.md`、源码和测试，确认文档描述仍与实现一致。

仓库文档应足以让后续 AI 接手；不要把聊天记录当作唯一依据。影响架构、发布边界或长期维护
方式的决定，应写入 `docs/internal/decisions/`。

## 事实优先级

发生冲突时按以下顺序核实并修正文档：

1. 当前源码和能证明行为的测试。
2. `docs/internal/contracts/` 中的行为与 API 契约。
3. `docs/internal/` 下的架构、计划和平台实现说明。
4. `docs/public/` 中面向用户的使用文档。
5. `docs/archive/` 中的历史记录；归档资料不能作为当前实现依据。

## 修改要求

- 新增或改变脚本 API 时，同步更新内部 API 契约、公开函数页、`catalog.json` 和 API 总览。
- 改变 IDE 抓图取色器工作流时，先核对并更新其行为契约；纯视觉调整不虚构业务变化。
- 只把 `docs/public/` 当作发布白名单。内部文档和归档资料不得被网站或发布脚本自动带出。
- 文档只记录当前真实能力；计划中的内容必须明确标为未实现。
- 验证范围与改动风险相称。纯文档整理运行文档检查即可，不需要编译整个项目。
- 只有用户明确要求时才提交；不要自动推送远程仓库。
