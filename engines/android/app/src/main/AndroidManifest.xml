<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 设备 API 读取完整已安装应用列表、保持屏幕常亮、振动和读取电话卡信息所需权限。 -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_PHONE_STATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission android:name="android.permission.SEND_SMS" />
    <!-- Android 10 及以下访问 /sdcard/xiaoyv/scripts 的传统存储权限。 -->
    <uses-permission
        android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <uses-permission
        android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="29" />
    <!-- Android 11 及以上保留脚本真实文件路径所需的特殊授权。 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="false"
        android:icon="@drawable/ic_xiaoyv_launcher"
        android:label="@string/app_name"
        android:requestLegacyExternalStorage="true"
        android:roundIcon="@drawable/ic_xiaoyv_launcher"
        android:supportsRtl="true"
        android:usesCleartextTraffic="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".ScriptWebActivity"
            android:configChanges="keyboardHidden|orientation|screenSize"
            android:exported="false"
            android:theme="@style/ScriptWebTheme" />

        <service
            android:name=".AutomationAccessibilityService"
            android:exported="true"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>

            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/accessibility_service_config" />
        </service>

        <service
            android:name=".FloatingControlService"
            android:exported="false" />

        <service
            android:name=".ScriptDialogOverlayService"
            android:exported="false" />

        <service
            android:name=".ScriptHudService"
            android:exported="false" />

        <service
            android:name=".RootDaemonService"
            android:exported="false" />

        <service
            android:name=".EngineService"
            android:exported="false"
            android:process=":engine" />

        <service
            android:name=".EngineInputMethodService"
            android:exported="true"
            android:label="@string/engine_input_method_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:process=":engine">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>

            <meta-data
                android:name="android.view.im"
                android:resource="@xml/engine_input_method" />
        </service>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/script_file_paths" />
        </provider>

        <!-- 包内 HTML、JS、CSS、图片由 WebView 通过 content URI 读取，不在磁盘解压。 -->
        <provider
            android:name=".ScriptPackageResources"
            android:authorities="com.xiaoyv.engine.package"
            android:exported="false"
            android:grantUriPermissions="false" />
    </application>

</manifest>
