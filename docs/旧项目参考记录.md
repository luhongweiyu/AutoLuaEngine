# 旧项目参考记录

旧项目只作为架构边界参考，不作为当前功能文档。

可参考点：

- App 主进程负责界面和控制。
- 独立引擎进程负责脚本执行。
- Native / so 层承接底层高频能力。
- Java 互操作采用 native userdata + Java 反射后端，支持字段、方法、构造函数、
  table 类型转换和接口回调；当前项目已按该边界独立实现，未直接复用反编译代码。

旧项目里的具体 API、函数名和实现路线不代表当前项目已实现能力。当前真实实现以
`docs/ANDROID_SO_截图核心.md`、`docs/ANDROID_Java互操作.md`、
`docs/API_契约.md` 和源码为准。
