# 用户待办事项

当前没有长期固定的用户待办。开发和文档整理可以直接根据仓库中的现行契约继续进行。

只有遇到对应任务时才需要用户配合：

1. 真机或模拟器验证：打开目标设备，并确认 ADB 或局域网连接可用。
2. 需要 Root 能力验证：使用已授权的测试设备并保持 RootDaemon 可启动。
3. 新能力存在多种合理交互或兼容方案：由 AI 列出具体差异，再请用户决定。
4. 构建输出被其他进程占用：先告知用户，可由用户关闭占用程序或自行完成编译。

已解决事项不要继续留在本文件；长期技术决定写入 `internal/decisions/`，当前产品能力写入
契约和平台说明。
