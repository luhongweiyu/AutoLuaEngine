---
params: "无"
returns: "无"
---
<!-- 标准化脚本文档：保留并扩展原有正文，不删除既有说明。 -->

**方法名称：** 释放常亮锁。

**语法：** `unLockScreen()`

**参数说明：** 无。

| 返回值 | 说明 |
|---|---|
| 无 | 此方法不返回值。 |

**使用示例：**

```lua
unLockScreen()
```

**详细说明：**

释放 `m.lockScreen()` 获取的常亮锁。
