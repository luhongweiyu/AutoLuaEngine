---
params: ""
returns: ""
---

**方法名称：** 重启当前脚本。

**语法：** `restartScript()`

**参数说明：** 无。

| 返回值 | 说明 |
|---|---|
| 无 | 请求结束当前脚本，并由宿主按原启动参数重新运行。 |

**使用示例：**

```lua
restartScript()
```

**详细说明：**

这是异步的宿主控制请求；当前脚本不会在该调用后继续完成后续业务逻辑。
