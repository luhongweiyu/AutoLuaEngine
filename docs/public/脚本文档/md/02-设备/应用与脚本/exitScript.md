---
params: "无"
returns: "无"
---
<!-- 标准化脚本文档：保留并扩展原有正文，不删除既有说明。 -->

**方法名称：** 立即停止当前顶层脚本。

**语法：** `exitScript()`

**参数说明：** 无。

| 返回值 | 说明 |
|---|---|
| 无 | 此方法不返回值。 |

**使用示例：**

```lua
print("脚本即将停止")
exitScript()
```

**详细说明：**

立即停止当前顶层脚本，后续 Lua 语句不会执行。
